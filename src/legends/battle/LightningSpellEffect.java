/**
 * LightningSpellEffect lowers a monster’s dodge chance on hit.
 * The exact percentage drop is implemented later.
 */

package legends.battle;

import legends.characters.Monster;

public class LightningSpellEffect implements SpellEffect {

    @Override
    public void apply(Monster monster) {
        // Will reduce monster dodge chance later
    }
}